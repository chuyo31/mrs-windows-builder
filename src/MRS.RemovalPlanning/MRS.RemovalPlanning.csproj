<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!--
    Garantía arquitectónica (Parte 16 del prompt 06): este proyecto NO referencia
    MRS.DismEngine. RemovalPlanBuilder no puede ejecutar DISM aunque quisiera:
    el propio grafo de dependencias del compilador lo impide.
  -->
  <ItemGroup>
    <ProjectReference Include="..\MRS.ImageEngine\MRS.ImageEngine.csproj" />
    <ProjectReference Include="..\MRS.ComponentCatalog\MRS.ComponentCatalog.csproj" />
  </ItemGroup>

</Project>
