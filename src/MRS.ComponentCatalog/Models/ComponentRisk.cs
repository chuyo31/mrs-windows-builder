namespace MRS.ComponentCatalog.Models;

/// <summary>Riesgo estimado de tocar un componente. No es la protección en sí.</summary>
public enum ComponentRisk
{
    Unknown,
    Low,
    Medium,
    High,
    Critical,
}
