<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <!-- Solicita elevación (UAC) al arrancar: DISM necesita privilegios de administrador. -->
    <ApplicationManifest>app.manifest</ApplicationManifest>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\MRS.ImageEngine\MRS.ImageEngine.csproj" />
    <ProjectReference Include="..\MRS.DismEngine\MRS.DismEngine.csproj" />
    <ProjectReference Include="..\MRS.ComponentCatalog\MRS.ComponentCatalog.csproj" />
  </ItemGroup>

</Project>
