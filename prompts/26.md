# P26 — Corregir orden de preparación de boot.wim y eliminación de ReadOnly

Trabaja sobre:

C:\Users\B3RASCASA\Desktop\MRS-W11PRO-by_chuyo31

CONTEXTO

P25 fue aplicado y committeado como:

5ce972b

Tests P25:
440/440

El build completo terminó correctamente.

Sin embargo, al ejecutar una generación ISO REAL, MRS sigue detectando:

"boot.wim en el workspace todavía tiene el atributo ReadOnly ... No se intentará montar."

La UI queda en:

Etapa: Preparando boot.wim
20 %
Fase 5/10

El terminal llega hasta:

Comprobando copia de boot.wim en el workspace...
OK boot.wim listo en el workspace...

Y posteriormente falla porque el boot.wim sigue teniendo ReadOnly.

PRUEBA MANUAL DEFINITIVA

El boot.wim real de la ISO tiene:

692758253 bytes

El boot.wim copiado al workspace tiene:

692758253 bytes

DISM /Get-WimInfo funciona correctamente.

El problema se reprodujo así:

1. El boot.wim del workspace tenía:
   ReadOnly, Archive

2. Se ejecutó:
   attrib -R "<workspace>\sources\boot.wim"

3. El archivo quedó:
   Archive

4. Se ejecutó:
   DISM /English /Mount-Wim ...

5. El montaje terminó correctamente al 100 %.

Por tanto:

- El WIM es válido.
- La copia es válida.
- DISM funciona.
- El atributo ReadOnly impide el montaje.
- El código debe eliminar ReadOnly ANTES de la validación que actualmente está rechazando el archivo.

OBJETIVO

Audita P25 y corrige el flujo real de producción.

NO asumas que P25 está correctamente integrado solo porque sus tests pasan.

Quiero que encuentres exactamente dónde ocurre esta secuencia:

Copiar boot.wim
→ validar boot.wim
→ detectar ReadOnly
→ abortar

y la cambies por:

Copiar boot.wim
→ preparar boot.wim de trabajo
→ eliminar ReadOnly
→ verificar que ReadOnly ya no existe
→ validar WIM
→ Mount-Wim

IMPORTANTE

La eliminación de ReadOnly debe ejecutarse sobre la COPIA DE TRABAJO, nunca sobre el archivo original de la ISO.

No modificar:

E:\sources\boot.wim
H:\sources\boot.wim

ni ningún archivo de la ISO montada.

IMPLEMENTACIÓN

Revisa específicamente:

- BootWimProvisioner
- BootWimModifier
- IsoTreeCopier
- IsoGenerationPipeline
- cualquier método que copie o prepare boot.wim

Determina cuál de ellos es realmente responsable de dejar el archivo listo para Mount-Wim.

No añadas simplemente otro "attrib -R" en un lugar arbitrario.

La preparación debe ocurrir en el punto correcto del flujo.

Preferir una API .NET apropiada para modificar FileAttributes en lugar de depender de ejecutar "attrib.exe", salvo que la arquitectura existente tenga una razón clara para usarlo.

La operación debe ser idempotente:

ReadOnly presente → eliminarlo.
ReadOnly ausente → continuar.

VALIDACIÓN

Después de preparar el archivo:

1. comprobar que existe;
2. comprobar tamaño razonable;
3. comprobar que FileAttributes no contiene ReadOnly;
4. validar Get-WimInfo;
5. SOLO DESPUÉS permitir Mount-Wim.

Si cualquiera de estas validaciones falla, no ejecutar Mount-Wim.

LOG

Quiero que el terminal muestre explícitamente algo parecido a:

Preparando boot.wim de trabajo...
Eliminando atributo ReadOnly de boot.wim...
OK boot.wim preparado para escritura.
Validando boot.wim...
OK boot.wim válido y preparado para montaje.
DISM: Mount-Wim de boot.wim iniciado

Si el archivo ya era writable:

OK boot.wim ya era writable.

No mostrar "boot.wim listo" antes de haber realizado la preparación.

TESTS

Revisa los tests de P25 y añade/ajusta pruebas para demostrar el ORDEN correcto de las operaciones.

Como mínimo:

1. Copia ReadOnly → preparación elimina ReadOnly → validación continúa → Mount-Wim puede ejecutarse.

2. Copia ya writable → no falla.

3. Archivo inexistente → falla antes de Mount-Wim.

4. Archivo demasiado pequeño → falla antes de Mount-Wim.

5. WIM inválido → falla antes de Mount-Wim.

6. El archivo original nunca es modificado.

7. Test específico que demuestre que la validación de ReadOnly NO ocurre antes de la fase de preparación.

IMPORTANTE SOBRE LOS FIXTURES

Los fixtures "fake boot.wim" existentes son de pruebas unitarias.

No los conviertas en WIM reales ni elimines las protecciones de seguridad para hacer pasar los tests.

No introduzcas excepciones especiales para los fixtures en código de producción.

VALIDACIÓN FINAL

Ejecuta:

dotnet test

Después:

dotnet build

sobre:

MRS-Windows-Builder.sln

Debe quedar todo limpio.

Comprueba especialmente que MRS.WindowsBuilder.exe no esté abierto bloqueando el build.

Actualiza:

prompts\26-resultado.md

con:

- causa exacta encontrada;
- flujo anterior;
- flujo corregido;
- archivos modificados;
- tests;
- resultado del build;
- commit recomendado.

NO GENERES UNA ISO.

NO CAMBIES:

- install.wim
- RemovalEngine
- PostInstall
- oscdimg
- InstallationOptions
- perfiles
- catálogo

salvo que sea estrictamente necesario para esta corrección.

Al finalizar, muestra un resumen conciso de la corrección y el resultado de tests/build.