Trabaja sobre el proyecto:
C:\Users\B3RASCASA\Desktop\MRS-W11PRO-by_chuyo31

CONTEXTO

La primera generación real de ISO llegó hasta la fase de preparación de boot.wim y falló al ejecutar Mount-Wim.

Investigación realizada:

- ISO montada en E: y H:.
- E:\sources\boot.wim es válido.
- H:\sources\boot.wim es válido.
- Ambos tienen 692758253 bytes.
- DISM /Get-WimInfo funciona correctamente en ambos.
- El boot.wim copiado por MRS al workspace real también tiene 692758253 bytes.
- DISM /Get-WimInfo funciona correctamente sobre esa copia.
- La copia inicialmente tenía:
  ReadOnly, Archive
- Se ejecutó:
  attrib -R "<workspace>\sources\boot.wim"
- El archivo quedó:
  Archive
- Inmediatamente después, el mismo boot.wim se pudo montar manualmente con:
  DISM /English /Mount-Wim /WimFile:"..." /Index:1 /MountDir:"..."
- El montaje llegó al 100 % correctamente.
- Ese montaje manual ya fue desmontado correctamente con:
  DISM /English /Unmount-Wim /MountDir:"..." /Discard

LOG DEL FALLO ORIGINAL

DISM intentó:

/Mount-Wim
/WimFile:"...\iso-workspaces\fa04747889fa4386ba0ac26f80a71ba8\sources\boot.wim"
/Index:1
/MountDir:"...\iso-workspaces\fa04747889fa4386ba0ac26f80a71ba8\mount"

Y devolvió:

Fail to flush file buffers
HRESULT=0x80070006
"WIM open failed with access denied."
hr=0xc1510111

DIAGNÓSTICO CONFIRMADO

El boot.wim de trabajo conserva ReadOnly al ser preparado/copiarlo desde el árbol de la ISO. Al quitar ReadOnly manualmente, DISM puede montarlo correctamente.

OBJETIVO

Corregir MRS para que la copia de trabajo de boot.wim sea preparada correctamente antes de Mount-Wim.

REQUISITOS

1. Localiza en el código el punto correcto donde IsoTreeCopier / BootWimProvisioner / BootWimModifier prepara el boot.wim de trabajo.

2. El boot.wim ORIGINAL de la ISO debe permanecer siempre intacto.

3. Después de copiar el boot.wim al workspace, elimina explícitamente el atributo ReadOnly SOLO de la copia de trabajo.

4. La operación debe ser idempotente:
   - si tiene ReadOnly, quitarlo;
   - si ya es writable, continuar sin error.

5. No dependas de que el usuario ejecute "attrib -R" manualmente.

6. Antes de Mount-Wim, valida:
   - el archivo existe;
   - su tamaño no es sospechosamente pequeño;
   - no tiene ReadOnly;
   - el archivo es un WIM válido usando la infraestructura DISM existente cuando corresponda.

7. No inventes una validación que duplique innecesariamente operaciones DISM costosas. Reutiliza las abstracciones existentes del proyecto.

8. Mantén el flujo actual:
   Preparación → Mount-Wim → modificación → validación → Commit/Discard.

9. No cambies install.wim.

10. No cambies PostInstall.

11. No cambies oscdimg.

12. No cambies RemovalEngine.

13. No cambies InstallationOptions.

14. No cambies el resto del pipeline salvo lo estrictamente necesario para solucionar este problema.

15. Mantén:
   - logging;
   - cancelación;
   - limpieza;
   - manejo de errores;
   - preservación del workspace ante fallos.

16. Añade logging claro, por ejemplo:
   "Preparando boot.wim de trabajo..."
   "Eliminando atributo ReadOnly de boot.wim..."
   "boot.wim preparado para montaje."

TESTS

Añade tests unitarios apropiados para cubrir:

- boot.wim con ReadOnly → se elimina ReadOnly correctamente.
- boot.wim ya writable → no falla.
- archivo inexistente → error claro.
- archivo demasiado pequeño/inválido → no intentar Mount-Wim.
- el archivo original de entrada no se modifica.

IMPORTANTE SOBRE TESTS

Los tests actuales contienen fixtures como "fake boot.wim".

NO trates "fake boot.wim" como un WIM real en una prueba de integración.

Para pruebas unitarias utiliza abstracciones/mocks apropiados.

No rompas los tests existentes.

VALIDACIÓN FINAL

Ejecuta:

dotnet test

sobre toda la solución.

Después:

dotnet build

sobre toda la solución.

Comprueba que no existen errores de compilación.

Si MRS.WindowsBuilder.exe está abierto y bloquea el build, ciérralo antes de compilar.

RESULTADO

Crea:

prompts\25-resultado.md

El resultado debe incluir brevemente:

- causa encontrada;
- archivos modificados;
- solución aplicada;
- tests ejecutados y resultado;
- build ejecutado y resultado;
- cualquier incidencia.

No generes todavía una nueva ISO automáticamente.

No hagas cambios adicionales fuera del alcance de este prompt.

Al terminar, muestra un resumen conciso y el commit recomendado.