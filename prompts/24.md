# PROMPT 24 — CONECTAR UI CON EL PIPELINE REAL DE GENERACIÓN ISO

Proyecto:
C:\Users\B3RASCASA\Desktop\MRS-W11PRO-by_chuyo31

CONTEXTO

El proyecto ya tiene implementado:

- MRS.ISOEngine
- IsoGenerationPipeline (P19)
- InstallationOptions
- BootWimProvisioner / BootWimModifier
- LabConfigApplier
- AutounattendGenerator
- PostInstall
- IsoTreeCopier
- OscdimgRunner
- GenerationWorkspaceValidator
- RemovalEngine
- WorkingImageFactory

P23 acaba de permitir un RemovalPlan vacío:

    TotalSelected == 0

y la UI muestra correctamente:

    SIN ELIMINACIONES DE COMPONENTES

con "Confirmar plan" habilitado.

PROBLEMA ACTUAL

Necesitamos verificar y completar el puente entre la UI WPF y el
IsoGenerationPipeline real.

NO asumir que el pipeline está correctamente conectado.
Primero inspeccionar el código actual.

OBJETIVO

Conseguir que desde la UI pueda iniciarse UNA ÚNICA generación real de ISO
utilizando el IsoGenerationPipeline existente de MRS.ISOEngine.

No crear un segundo pipeline.

CASO PRINCIPAL DE ESTA FASE

Plan con:

    0 componentes seleccionados
    0 acciones de eliminación

Debe poder generar:

ISO original
→ workspace de generación
→ copia completa del árbol ISO
→ modificación de boot.wim según InstallationOptions
→ preparación de install.wim Pro
→ SIN ejecutar RemovalEngine si no hay eliminaciones
→ PostInstall
→ validación final
→ oscdimg
→ ISO final

PRIMER PASO OBLIGATORIO: AUDITORÍA DEL CABLEADO ACTUAL

Inspeccionar:

- MainWindow.xaml.cs
- MainWindow.xaml
- MRS.ISOEngine
- IsoGenerationPipeline
- modelos/options usados por el pipeline
- flujo actual de PlanConfirmButton_Click
- cualquier método que ya intente generar ISO

Determinar exactamente:

1. Qué método del pipeline debe invocar la UI.
2. Qué parámetros necesita.
3. Qué objetos ya existen en MainWindow y pueden reutilizarse.
4. Qué datos faltan.
5. Si actualmente existe alguna implementación parcial o duplicada.

NO hacer cambios hasta haber identificado esto.

INTEGRACIÓN

Una vez localizado el punto correcto:

1. Reutilizar IsoGenerationPipeline existente.

2. No duplicar:
   - creación de workspace;
   - copia del árbol ISO;
   - boot.wim;
   - install.wim;
   - PostInstall;
   - validación;
   - oscdimg.

3. Pasar correctamente:
   - ISO original;
   - índice de edición seleccionado;
   - RemovalPlan;
   - InstallationOptions;
   - archivos PostInstall configurados;
   - configuración necesaria del pipeline.

4. Para:

       RemovalPlan.TotalSelected == 0

   no ejecutar RemovalEngine ni montar/commit de una imagen solo para simular
   eliminaciones.

5. Para:

       RemovalPlan.TotalSelected > 0

   conservar el comportamiento de eliminación existente y utilizar el flujo
   previsto por el pipeline.

6. Un plan con componentes seleccionados pero todos bloqueados NO debe generar.

7. No cambiar las reglas de protección ni el RemovalPlan.

UI

Actualmente el botón de la pantalla PLAN DE MODIFICACIÓN se llama
"Confirmar plan".

Adaptarlo para que represente correctamente la siguiente acción.

Cuando:

    TotalSelected == 0

mostrar:

    GENERAR ISO

Cuando haya eliminaciones permitidas:

    CONFIRMAR Y GENERAR ISO

Si todos los seleccionados están bloqueados:

    permanecer bloqueado.

El texto debe dejar claro que "0 eliminaciones" es válido.

GENERACIÓN

Crear/usar una pantalla de ejecución específica para la generación ISO,
reutilizando el estilo visual de la pantalla "CREANDO IMAGEN".

Debe mostrar:

- título: GENERANDO ISO
- barra de progreso 0–100 %
- fase actual
- porcentaje
- mensaje
- terminal/log existente
- botón Cancelar cuando sea seguro cancelar
- botón Cerrar al finalizar

No bloquear la interfaz.

Si IsoGenerationPipeline ya dispone de progreso, reutilizarlo.
Si no dispone de progreso suficiente, añadir una abstracción de progreso
independiente de WPF, siguiendo el patrón de P10/P22.

No inventar progreso interno de DISM.

FASES VISIBLES

Como mínimo:

    Preparando generación
    Validando entorno
    Preparando workspace
    Copiando árbol ISO
    Preparando boot.wim
    Preparando install.wim
    Aplicando eliminaciones (solo si existen)
    Preparando PostInstall
    Validación final
    Creando ISO con oscdimg
    Finalizando
    100 % completado

Si una fase no aplica, no mostrarla como ejecutada falsamente.

CASO 0 ELIMINACIONES

Debe quedar explícitamente reflejado en el log:

    [REMOVAL] Sin eliminaciones de componentes.

Y continuar directamente con las fases necesarias de generación.

NO ejecutar operaciones DISM de eliminación innecesarias.

INSTALATION OPTIONS

Usar exactamente el estado actual de InstallationOptions.

No cambiar sus valores por defecto.

Mantener:

- cuenta local;
- instalación/offline OOBE;
- bypass TPM;
- bypass Secure Boot;
- bypass CPU;
- bypass RAM.

Storage bypass continúa sin implementar.

Si StorageBypass está activado, el pipeline debe rechazar la generación de forma
segura, tal como ya establece P15/P16.

POSTINSTALL

Reutilizar MRS.PostInstall existente.

No crear scripts alternativos.

Debe conservar:

- .NET Desktop Runtime 8.0.26 x64
- PCPI-Retro-Minimals-Portable-0.0.5.exe
- SetupComplete.cmd

VALIDACIÓN

Antes de generar:

- validar ISO;
- validar edición;
- validar arquitectura;
- validar workspace;
- validar boot.wim;
- validar install.wim;
- validar opciones;
- validar PostInstall;
- validar oscdimg.

Si falla una precondición, NO comenzar modificaciones.

Si falla durante generación:

- informar fase;
- conservar diagnóstico/workspace según las reglas existentes;
- no borrar ciegamente workspaces;
- no dejar montajes DISM;
- no generar una ISO final falsa.

SALIDA

La ISO final debe tener una ruta de salida determinista y visible para el
usuario.

Mostrar al finalizar:

    ISO generada correctamente
    Ruta: <ruta>

No abrir automáticamente la ISO.

NO TOCAR

No modificar funcionalidad de:

- ComponentCatalog
- ProtectionEngine
- ProfileEngine
- SecurityOptions
- InstallationOptions salvo integración
- RemovalPlanning
- RemovalEngine salvo lo estrictamente necesario para la ruta existente
- LabConfigApplier
- AutounattendGenerator
- PostInstall
- IsoTreeCopier
- OscdimgRunner

No reescribir IsoGenerationPipeline si ya contiene la lógica necesaria.

TESTS

Añadir tests para la integración/lógica nueva:

1. Plan vacío → no ejecuta RemovalEngine.
2. Plan vacío → continúa hacia generación ISO.
3. Plan con eliminaciones → conserva el flujo de eliminación.
4. Plan con seleccionados bloqueados → no genera.
5. Storage bypass → generación rechazada.
6. InstallationOptions se transmite sin alteraciones.
7. La UI no crea un segundo pipeline.
8. Fallo de prevalidación no inicia generación.
9. Cancelación/fallo no informa éxito falso.

Si el proyecto no permite testear directamente WPF, testear la capa de
orquestación independiente de WPF.

EJECUTAR

    dotnet test

    dotnet build

Deben terminar correctamente.

DOCUMENTACIÓN

Crear:

    prompts\24-resultado.md

Documentar:

- auditoría del cableado existente;
- punto exacto de integración encontrado;
- cambios realizados;
- flujo 0 eliminaciones;
- flujo con eliminaciones;
- progreso de generación;
- validaciones;
- tests;
- build.

IMPORTANTE

No implementar optimizaciones de tamaño ISO todavía.
No eliminar componentes automáticamente.
No modificar reglas de protección.
No añadir nuevas capacidades fuera de este puente UI → IsoGenerationPipeline.

Al finalizar:

- indicar commit recomendado;
- indicar claramente si la generación real queda lista para probar;
- indicar cualquier limitación real que todavía impida generar una ISO.